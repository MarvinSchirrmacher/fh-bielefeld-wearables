<Label>:
    font_size: sp(25)
    halign: 'center'

<TabbedPanel>:
    do_default_tab: False
    tab_pos: 'right_top'
    tab_height: pt(40)
    tab_width: self.height / 3

<Management>:
    canvas.before:
        Color:
            rgba: .949, .953, .954, 0
        Rectangle:
            size: self.size
            pos: self.pos

    TabbedPanelItem:
        text: 'Pack'

        GridLayout:
            cols: 2

            Button:
                text: 'Start reading'
                size_hint_y: None
                height: pt(40)
                callback: root.content_management.start()
            Button:
                text: 'Stop reading'
                size_hint_y: None
                height: pt(40)
                callback: root.content_management.stop()
            ListView:
                item_strings: root.settings.current_configuration
            ListView:

    TabbedPanelItem:
        text: 'Trim'

        Label:
            text: 'Trimming mode is active.\nWatch for the LEDs on the shoulder straps.'

    TabbedPanelItem:
        text: 'Configure'

        BoxLayout:
            orientation: 'vertical'

            BoxLayout:
                orientation: 'horizontal'

                Label:
                    text: 'Gender'
                Label:
                    text: str(root.settings.gender)

            BoxLayout:
                orientation: 'horizontal'

                Label:
                    text: 'Birthday'
                Label:
                    text: str(root.settings.birthday)

            BoxLayout:
                orientation: 'horizontal'

                Label:
                    text: 'Height'
                Label:
                    text: str(root.settings.height)

            BoxLayout:
                orientation: 'horizontal'

                Label:
                    text: 'Weight'
                Label:
                    text: str(root.settings.weight)